# Generated by OpsClaude — upload-processor (dev)

terraform {
  backend "s3" {
    bucket         = "garda-terraform-state"
    key            = "dev/upload-processor/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
