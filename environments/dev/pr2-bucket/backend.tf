# Generated by OpsClaude — pr2-bucket (dev)

terraform {
  backend "s3" {
    bucket         = "garda-terraform-state"
    key            = "dev/pr2-bucket/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
