# Generated by OpsClaude — backups (dev)

terraform {
  backend "s3" {
    bucket         = "garda-terraform-state"
    key            = "dev/backups/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
