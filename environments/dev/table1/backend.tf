# Generated by OpsClaude — table1 (dev)

terraform {
  backend "s3" {
    bucket         = "garda-terraform-state"
    key            = "dev/table1/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
